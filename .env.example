DATABASE_URL=
DATABASE_URL_UNPOOLED=
AUTH_SECRET=
ADMIN_EMAIL=
# Sends GT email verification codes. Without it, codes are printed to the
# server console instead of emailed. Get one at https://resend.com
RESEND_API_KEY=
# Optional; defaults to Resend's shared onboarding@resend.dev sender.
EMAIL_FROM=
