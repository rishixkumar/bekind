DATABASE_URL=
DATABASE_URL_UNPOOLED=
AUTH_SECRET=
ADMIN_EMAIL=
# Optional / unused for signup right now (GT email format is enough).
# Kept for a future mailer if needed. Get one at https://resend.com
RESEND_API_KEY=
# Optional; defaults to Resend's shared onboarding@resend.dev sender.
EMAIL_FROM=
